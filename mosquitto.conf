persistence true
persistence_location /mosquitto/data/
log_dest file /mosquitto/log/mosquitto.log
listener 1883
socket_domain ipv4
# Mosquitto >= 2.0 only permits user authenticated logins
# We allow anonymous login to test the enviorement
allow_anonymous true
